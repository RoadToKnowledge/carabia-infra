apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: carabia-dataprotection-pvc
  namespace: prod
spec:
  storageClassName: efs-sc
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi