apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: carabia-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: carabia-service
            port:
              number: 80
              # ghdgh
