apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: carabia-dataprotection-pvc
  namespace: dev
spec:
  storageClassName: efs-sc
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi